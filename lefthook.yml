pre-commit:
  parallel: true
  commands:
    lint:
      run: npm run lint
      skip:
        - merge
        - rebase
    types:
      run: npm run types
      skip:
        - merge
        - rebase
