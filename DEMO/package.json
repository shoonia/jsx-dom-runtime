{
  "name": "jsx-dom-runtime-demo",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "start": "parcel src/index.html --no-source-maps --no-cache --open",
    "build": "rm -rf dist && parcel build --no-source-maps --public-url /jsx-dom-runtime/DEMO/dist src/index.html"
  },
  "dependencies": {
    "jsx-dom-runtime": "^0.17.1"
  },
  "devDependencies": {
    "parcel": "^2.0.0"
  },
  "browser": {
    "engines": {
      "browsers": [
        "> 1%",
        "not dead"
      ]
    }
  },
  "license": "MIT"
}
