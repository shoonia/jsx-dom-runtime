{
  "name": "jsx-dom-runtime-demo",
  "version": "1.0.0",
  "description": "demo",
  "private": true,
  "type": "module",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel src/index.html --no-source-maps --no-cache --open",
    "build": "rm -rf dist .parcel-cache && parcel build --no-source-maps --no-cache src/index.html"
  },
  "dependencies": {
    "jsx-dom-runtime": "^0.39.0"
  },
  "devDependencies": {
    "parcel": "^2.8.3",
    "postcss": "^8.4.23",
    "postcss-modules": "^4.3.1"
  },
  "browser": {
    "engines": {
      "browsers": [
        "> 1%",
        "not dead"
      ]
    }
  },
  "targets": {
    "default": {
      "publicUrl": "/jsx-dom-runtime/"
    }
  },
  "license": "MIT"
}
