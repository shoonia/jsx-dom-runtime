export let isNotNil = (val) => val != null;
export let isString = (val) => typeof val === 'string';
export let isNumber = (val) => typeof val === 'number';
export let isFunction = (val) => typeof val === 'function';
export let isBoolean = (val) => typeof val === 'boolean';

export let isArray = Array.isArray;
export let doc = document;
